{
	"name": "@api/middlewares",

	"version": "1.0.0",

	"description": "",

    "dependencies" : {
		"@api/databases" : "^1.0.0"
	},

	"types": "types/packages/api/middlewares/src/index.d.ts",

	"exports":  {
		".": {
			"import": ["./src/index.ts"],
			"require": ["./dist/index.js"]
		}
	},

	"scripts": {
		"transpile": "babel -d dist/ --extensions .ts src/",
		"emitDeclarations": "tsc --declaration --emitDeclarationOnly",
		"build": "npm run emitDeclarations && npm run transpile",
		"check-types": "tsc --noEmit"
	}
}	
